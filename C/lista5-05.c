int main() 
{
   
   int matriz[TAM][TAM];
   int matrizModificada[TAM][TAM];
   int i, j, A,B;


   puts( "Entre com a matriz 5x5:\n" );
		
   for( i = 0; i < 5; i++ )
   { 
      for( j = 0; j < 5; j++ )
      {
	  scanf( "%d", &matriz[i][j]);

      }	
   }
   puts( "Agora entre com os valores de A:" );
   scanf( "%d",A );
   puts( "Agora entre com os valores de B:" );
   scanf( "%d",B );
   
   if ( A >= 0 && A < 5) && ( B >= 0 && B < 5)
   {
	   for( i = A; i < 5; i++ )
	   { 
		  for( j = 0; j < 5; j++ )
		  {
		  
		  }	
	   }

	   for( j = 0; i < 5; i++ )
	   { 
	      
	   }	
	}
  
   printf( " " );
   printf( " " );

	
 
}