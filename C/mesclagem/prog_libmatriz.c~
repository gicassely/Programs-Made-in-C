#include <stdio.h>
#include <string.h>
#include <stdlib.h>

#include "prog_libmatriz.h"
#include "libmatriz.h"

int main ()
{
  int m=0;
  char **matriz;
  int linhas,colunas;
  
  cria_m(matriz, linhas, colunas);


  while (m != 2)
    {      
      printf("o que deseja fazer?\n");
      printf("1- Redimensionar matriz\n");
      printf("2- Sair do programa\n");
      scanf("%d",&m);
      
      if (m == 1)
	{
	  redimensiona_m (matriz,linhas,colunas);
	  for (i=0;i<
	}
    }
}
